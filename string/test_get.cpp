#include "string.h"
#include <cassert>

//===========================================================================
int main ()
{
  {
    // setup
    string str;
    char t;
    // test
    //    string::get(std::istream& in, t);

    // Verify
    //assert(str == "t");
  }
}
